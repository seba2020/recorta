name: Verify Recorta

on:
  push:
  pull_request:

jobs:
  checks:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Verify crop engine
        run: ./check.sh
      - name: Build application
        run: swift build -c release
